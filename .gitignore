# Python
__pycache__/
*.pyc
*.pyo
venv/
.venv/
.venv-*/
venv-*/
env/
env-*/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Environment / secrets
.env
.env.local
.env.*.local

# OS
.DS_Store
Thumbs.db
Desktop.ini
ehthumbs.db
$RECYCLE.BIN/

# Editor
.vscode/
.idea/
*.swp
*.swo
*~
*.tmp
*.temp
*.bak

# Redundant archive / stale duplicate — see README, not part of the app
aura-mvp.zip
/aura-mvp/
/main.py

# Real visitor photos used for manual recognition testing — personal photos,
# not project source data, never meant to be committed.
/test_photos/

# Runtime cache — reference images fetched from Wikimedia Commons for the
# hybrid pipeline's visual-verification step (see backend/app/main.py);
# rebuildable, not source data.
/backend/.reference_cache/

# Runtime cache — Recap PNG export's image-proxy endpoint (same rationale
# as .reference_cache/ above, different cache key: URL hash, not artwork id).
/backend/.image_proxy_cache/

# Point-in-time output of backend/scripts/latency_test.py -- rerun the
# script for current numbers rather than trusting a stale committed copy.
/latency_test_results.json

# Louvre importer test-run output (raw/normalized records, checkpoints,
# error logs) -- regenerable from the live source via
# backend/scripts/louvre_import.py, not itself source data. Explicitly NOT
# committed while this pilot is still test-scale and un-reviewed.
/backend/data/louvre/

# Local database snapshots and rollback material. These are intentionally
# retained on developer machines, never uploaded to Git.
/backups/
/local-archive/

# Research/benchmark/QA exports are generated by backend/scripts and retained
# locally as evidence. A small, explicitly force-added canonical fixture may
# still be tracked; this rule prevents new bulk output from becoming noise.
/exports/

# Lighthouse and browser-profile output generated during local performance QA.
/web/.baseline-*.json
/web/.after-*.json
/web/.final*.json
/web/.repeat-*.json
/.lighthouseci/
**/.lighthouseci/
/.lh-warm/
/web/.lh-warm/
lighthouse-*.json
lighthouse-*.html

# Runtime logs, caches and local helper/deployment state.
*.log
.cache/
*.cache
/.last_deploy_clean_path
/codex-switch.cmd
/codex-switch.ps1

.vercel
