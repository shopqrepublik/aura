import type { Metadata } from "next";import Link from "next/link";import {notFound} from "next/navigation";import SeoNav from "@/components/seo/SeoNav";import {LOCALES,alternatesFor,museums,type SeoLocale} from "@/lib/seo-content";
export async function generateMetadata({params}:{params:Promise<{locale:string}>}):Promise<Metadata>{const{locale}=await params;if(!LOCALES.includes(locale as SeoLocale))return{};const l=locale as SeoLocale;const title=l==="fr"?"Guides des musées de France — ELYIO":l==="zh-hans"?"法国博物馆指南 — ELYIO":"France Museum Guides — ELYIO";const description=l==="fr"?"Guides ELYIO pour 12 musées sélectionnés, avec œuvres majeures, conseils de visite et liens vers les histoires d’art.":l==="zh-hans"?"ELYIO 的 12 家精选法国博物馆指南，包含重点作品、参观建议与艺术故事。":"ELYIO guides to 12 curated French museums, with highlights, visit guidance and artwork stories.";return{title,description,alternates:alternatesFor(`/${l}/museums`)}}
export default async function MuseumsPage({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!LOCALES.includes(locale as SeoLocale))notFound();const l=locale as SeoLocale;return <><SeoNav locale={l}/><main className="seo-content"><nav className="seo-breadcrumbs"><Link href={`/${l}`}>ELYIO</Link> / {l==="fr"?"Musées":l==="zh-hans"?"博物馆":"Museums"}</nav><p className="seo-kicker">France · 12</p><h1>{l==="fr"?"Guides de musées":l==="zh-hans"?"博物馆指南":"Museum guides"}</h1><div className="seo-grid">{museums.map(m=><article key={m.id}><p>{m.city}</p><h2><Link href={`/${l}/museums/${m.slug}`}>{m.name}</Link></h2><p>{m.intro[l]}</p></article>)}</div></main></>}
